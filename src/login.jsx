import React, {Component} from "react";
import {render} from "react-dom";

const LoginForm  = () => {
    return (
        <div>
            Ze Login Stuffs.
        </div>
    );
};

render(<div>meow</div>, document.getElementById("login-app"));