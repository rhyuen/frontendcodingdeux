import React, {Component} from "react";

export default () => (
    <footer>
        <section className = "footer__panel">
            <div>
                What I do 
            </div>
        </section>
        <section className = "footer__panel">
            <div>
                Contact
            </div>
        </section>
    </footer>
);